import React from "react";
import styled from "styled-components";

const Main = () => {
  return (
    <Container>
      <h2>main</h2>
    </Container>
  );
};

const Container = styled.div``;
export default Main;
